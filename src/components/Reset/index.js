import React from "react";

const Reset = ({ handleClick }) => (
    <button
        className="btn btn-danger"
        onClick={ handleClick }
    >
        Reset
    </button>
);

export default Reset;